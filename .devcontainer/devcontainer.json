{
    "name": "torch-mlir",

    "build": {
        "dockerfile": "../build_tools/docker/Dockerfile",
        "context": ".."
    },

    "workspaceFolder": "/workspace/torch-mlir",

    "mounts": [
        "source=${localWorkspaceFolder},target=/workspace/torch-mlir,type=bind",
        "source=${localEnv:HOME}/.claude,target=/root/.claude,type=bind,consistency=cached"
    ],

    "runArgs": [
        "--init",
        "--cap-add=SYS_PTRACE",
        "--security-opt=seccomp=unconfined"
    ],

    "remoteUser": "root",

    "customizations": {
        "vscode": {
            "extensions": [
                "llvm-vs-code-extensions.vscode-clangd",
                "llvm-vs-code-extensions.vscode-mlir",
                "llvm-vs-code-extensions.lldb-dap",
                
                "Mlir-Visualizer.mlir-tree-viewer",

                "ms-vscode.cmake-tools",
                "ms-vscode.cpptools",

                "matepek.vscode-catch2-test-adapter",

                "eamodio.gitlens",
                "EditorConfig.EditorConfig",
                
                "Anthropic.claude-code"
            ],

            "settings": {
                "editor.formatOnSave": true,

                "[c]": {
                    "editor.defaultFormatter": "llvm-vs-code-extensions.vscode-clangd"
                },

                "[cpp]": {
                    "editor.defaultFormatter": "llvm-vs-code-extensions.vscode-clangd"
                },

                "C_Cpp.intelliSenseEngine": "disabled",

                "clangd.arguments": [
                    "--background-index",
                    "--clang-tidy",
                    "--completion-style=detailed",
                    "--header-insertion=iwyu",
                    "--cross-file-rename",
                    "--pch-storage=memory"
                ],

                "cmake.useCMakePresets": "never",

                "cmake.generator": "Ninja",
                "cmake.configureOnOpen": true,
                "cmake.sourceDirectory": "${workspaceFolder}/externals/llvm-project/llvm",
                "cmake.buildDirectory": "${workspaceFolder}/build",

                "cmake.configureSettings": {
                    "CMAKE_BUILD_TYPE": "Debug",
                    "CMAKE_EXPORT_COMPILE_COMMANDS": "ON",
                    "CMAKE_C_COMPILER": "/usr/bin/clang",
                    "CMAKE_CXX_COMPILER": "/usr/bin/clang++",
                    "LLVM_ENABLE_PROJECTS": "mlir",
                    "LLVM_EXTERNAL_PROJECTS": "torch-mlir",
                    "LLVM_EXTERNAL_TORCH_MLIR_SOURCE_DIR": "${workspaceFolder}",
                    "LLVM_TARGETS_TO_BUILD": "host",
                    "LLVM_ENABLE_ASSERTIONS": "ON",
                    "LLVM_BUILD_TESTS": "OFF",
                    "MLIR_ENABLE_BINDINGS_PYTHON": "ON",
                    "Python3_FIND_VIRTUALENV": "ONLY",
                    "Python_FIND_VIRTUALENV": "ONLY"
                },

                "cmake.defaultBuildTarget": "tools/torch-mlir/all",

                "cmake.additionalKits": [],
                "cmake.preferredGenerators": ["Ninja"],

                "cmake.debugConfig": {
                    "type": "lldb-dap"
                },

                "testMate.cpp.debug.configTemplate": {
                    "type": "lldb-dap",
                    "program": "${exec}",
                    "args": "${argsArray}",
                    "cwd": "${cwd}",
                    "env": "${envObj}",
                    "sourceFileMap": "${sourceFileMapObj}"
                },

                "files.associations": {
                    "*.td": "tablegen"
                },

                "terminal.integrated.defaultProfile.linux": "bash"
            }
        }
    }
}
